
export enum SYS_ROLE {
    user,
    admin,
    superAdmin
}

export enum GENDER {
    male,
    female
}

export enum USER_AGENT {
    google,
    local
}

export enum REACTION {
    like,
    love,
    haha,
    wow,
    sad,
    angry
}
